{
    "name": "@use-midi/root",
    "version": "1.0.0",
    "author": "tsei",
    "license": "MIT",
    "private": true,
    "description": "🖖Cross-platform midi engine for React",
    "repository": "tseijp/use-midi",
    "homepage": "https://github.com/tseijp/use-midi#readme",
    "maintainers": [
        "tseijp (https://github.com/tseijp)"
    ],
    "workspaces": [
        "packages/*",
        "examples/*"
    ],
    "scripts": {
        "core@latest": "lerna add @docusaurus/core@latest --dev --scope @use-midi/docs",
        "preset@latest": "lerna add  @docusaurus/preset-classic@latest --dev --scope @use-midi/docs",
        "latest": "yarn run core@latest & yarn run preset@latest",
        "init": "npm i -g yarn lerna && npx lerna bootstrap",
        "start": "cd examples && cd docs && yarn start",
        "build": "cd examples && cd docs && yarn build",
        "deploy": "cd examples && cd docs && yarn deploy",
        "rollup": "cd packages && cd core && yarn rollup -c",
        "watch": "cd packages && cd core && yarn jest --watch --runInBand",
        "cov": "cd packages && cd core && yarn jest --coverage",
        "npm": "cd packages && cd core && yarn publish",
        "ls": "cd packages && cd core && npm pack --dry-run"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    }
}
