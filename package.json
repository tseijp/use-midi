{
    "name": "@use-midi/root",
    "version": "1.0.0",
    "author": "tsei",
    "license": "MIT",
    "private": true,
    "description": "🖖Cross-platform midi engine for React",
    "repository": "tseijp/use-midi",
    "homepage": "https://github.com/tseijp/use-midi#readme",
    "maintainers": [
        "tseijp (https://github.com/tseijp)"
    ],
    "workspaces": [
        "packages/*",
        "examples/*"
    ],
    "scripts": {
        "core@latest": "lerna add @docusaurus/core@latest --dev --scope @use-midi/docs",
        "preset@latest": "lerna add  @docusaurus/preset-classic@latest --dev --scope @use-midi/docs",
        "latest": "yarn run core@latest & yarn run preset@latest",
        "init": "npm i -g yarn lerna && npx lerna bootstrap",
        "start": "lerna exec --scope @use-midi/docs -- yarn start",
        "build": "lerna exec --scope @use-midi/docs -- yarn build",
        "deploy": "lerna exec --scope @use-midi/docs -- yarn deploy",
        "rollup": "lerna exec --scope use-midi -- yarn rollup -c",
        "npm": "lerna exec --scope use-midi -- yarn publish",
        "cov": "lerna exec --scope use-midi -- yarn jest --coverage",
        "watch": "lerna exec --scope use-midi -- yarn jest --watch --runInBand",
        "ls": "lerna exec --scope use-midi -- npm pack --dry-run"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    }
}
