---
sidebar_label: useNote
---

import { useNote } from 'use-midi/src'
import { Live } from '/components/Live'

<Live {...{useNote}}>
{`function App () {
  const bind = useNote(state => {})
  return null
}`}
</Live>
