---
sidebar_label: useMidi
---

import { useMidi } from 'use-midi/src'
import { Live } from '/components/Live'

<Live {...{useMidi}}>
{`function App () {
  const bind = useMidi({
    button: state => console.log(state),
    slider: state => console.log(state),
    note: state => console.log(state)
  })
  return <div />
}`}
</Live>
