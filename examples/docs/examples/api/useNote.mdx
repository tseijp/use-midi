---
sidebar_label: useNote
---

import { useNote, rma } from 'use-midi/src'
import { Live } from '/components/Live'

<Live {...{rma, useNote}}>
{`function App () {
  const device = e => [...e.target.inputs.keys()][1]
  const [logs, set] = React.useState([])
  const bind = useNote(state => set(p => [...p, 'bind' + state]), {device})
  return logs.map(log => <p key={log}>{log}</p>)
}`}
</Live>
