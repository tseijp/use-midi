---
sidebar_label: Vanilla
---

import { Live } from '/components/Live'
import { Midi, rma } from 'use-midi/src'

<Live {...{Midi, rma}}>
{`function () {
    const button = state => {}
    const slider = state => {}
    const note = state => {}
    const [state, set] = React.useState()
    React.useEffect(() => {
        rma(() => {
            const target = Array.from(rma.inputs)[0]
            const props = {button, slider, note}
            const midi = new Midi(target, props)
            set(midi)
        })
    }, [])
    return (
        <div></div>
    )
}`}
</Live>
